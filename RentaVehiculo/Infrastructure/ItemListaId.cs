namespace RentaVehiculo.UI.Infrastructure;

/// <summary>Elemento para mostrar en ComboBox (identificador + texto legible).</summary>
public sealed class ItemListaId
{
    public int Id { get; set; }
    public string Nombre { get; set; } = "";
}
